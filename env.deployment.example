# Environment Variables for Deployment
# Copy this file to set up your environment variables in Coolify or your deployment platform

# Database Configuration
# For SQLite (default development setup)
DATABASE_URL="file:./prisma/dev.db"
# For PostgreSQL (recommended for production)
# DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Application Environment
NODE_ENV="production"

# Better Auth Configuration
# REQUIRED: Strong secret for JWT signing and encryption (generate with: openssl rand -base64 32)
AUTH_SECRET="your-super-secret-auth-key-here-change-this-in-production"

# REQUIRED: Your application URL (update for your domain)
BETTER_AUTH_URL="https://your-app-domain.com"
NEXT_PUBLIC_BETTER_AUTH_URL="https://your-app-domain.com"

# REQUIRED: Relying Party ID for WebAuthn/Passkeys (domain without scheme)
# For localhost: use "localhost"
# For production: use your domain (e.g., "yourdomain.com")
RP_ID="your-domain.com"

# Google OAuth (Optional - for Google sign-in)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
NEXT_PUBLIC_GOOGLE_CLIENT_ID="your-google-client-id"

# Email Configuration (Optional - for email verification and password reset)
# Using Plunk SMTP service (recommended)
SMTP_HOST="smtp.useplunk.com"
SMTP_PORT="465"
SMTP_USERNAME="plunk"
SMTP_PASSWORD="your-plunk-api-key"
EMAIL_FROM="noreply@yourdomain.com"

# Deployment Configuration
PORT="3000"
NEXT_TELEMETRY_DISABLED="1"

# Development Configuration (Optional)
# SKIP_ENV_VALIDATION="false"
